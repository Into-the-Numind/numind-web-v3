/**
 * 浏览器插件相关占位常量（T8）。
 *
 * TODO(xhs-collector): 插件正式发布后替换为真实值。
 *   - EXTENSION_DOWNLOAD_URL：插件 zip 包下载地址（当前为占位，待 CI 产出）
 *   - EXTENSION_ID：已发布插件的 chrome runtime id（chrome.runtime.sendMessage 用）
 *   - EXTENSION_VIDEO_URL：图文/视频安装引导（当前为占位）
 */

/** 插件 zip 下载链接（占位，待替换） */
export const EXTENSION_DOWNLOAD_URL = '/downloads/xhs-collector-extension.zip'

/** 已发布插件的 chrome runtime id（占位，待替换） */
export const EXTENSION_ID = 'PLACEHOLDER_EXTENSION_ID'

/** 安装引导视频（占位，待替换） */
export const EXTENSION_VIDEO_URL = ''
